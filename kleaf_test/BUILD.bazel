load(":kleaf_test.bzl", "kleaf_test")

# Tests on Kleaf functionality using virtual device as a baseline.
kleaf_test(
    name = "kleaf_test",
    extras = [
        # keep sorted
        "//common-modules/virtual-device/kleaf_test/compdb_test",
    ],
)
